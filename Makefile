# GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug_x64
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug_x64)
  jogo_estatistica_config = debug_x64
  raylib_config = debug_x64

else ifeq ($(config),debug_x86)
  jogo_estatistica_config = debug_x86
  raylib_config = debug_x86

else ifeq ($(config),debug_arm64)
  jogo_estatistica_config = debug_arm64
  raylib_config = debug_arm64

else ifeq ($(config),release_x64)
  jogo_estatistica_config = release_x64
  raylib_config = release_x64

else ifeq ($(config),release_x86)
  jogo_estatistica_config = release_x86
  raylib_config = release_x86

else ifeq ($(config),release_arm64)
  jogo_estatistica_config = release_arm64
  raylib_config = release_arm64

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := jogo-estatistica raylib

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

jogo-estatistica: raylib
ifneq (,$(jogo_estatistica_config))
	@echo "==== Building jogo-estatistica ($(jogo_estatistica_config)) ===="
	@${MAKE} --no-print-directory -C build/build_files -f jogo-estatistica.make config=$(jogo_estatistica_config)
endif

raylib:
ifneq (,$(raylib_config))
	@echo "==== Building raylib ($(raylib_config)) ===="
	@${MAKE} --no-print-directory -C build/build_files -f raylib.make config=$(raylib_config)
endif

clean:
	@${MAKE} --no-print-directory -C build/build_files -f jogo-estatistica.make clean
	@${MAKE} --no-print-directory -C build/build_files -f raylib.make clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug_x64"
	@echo "  debug_x86"
	@echo "  debug_arm64"
	@echo "  release_x64"
	@echo "  release_x86"
	@echo "  release_arm64"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   jogo-estatistica"
	@echo "   raylib"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"